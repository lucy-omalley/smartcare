/** Re-export adventure gating as poster gating for backward compatibility */
export {
  getPosterFeatures,
  assertCanCreatePoster,
  assertCanUseAiPoster,
  assertThemeAllowed,
  assertLayoutAllowed,
  assertFormatAllowed,
} from "@/lib/adventure/gating";
